spring:
  application:
    name: noteservice
  cloud:
    config:
      uri: http://localhost:8094
server:
  port: 8092
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8090/eureka/
    register-with-eureka: true
    fetch-registry: true
    